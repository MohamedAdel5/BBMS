@model IEnumerable<BBMS.donor_health_info>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.blood_pressure)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.glucose_level)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.notes)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.donor.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.hospital.username)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.blood_pressure)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.glucose_level)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.notes)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.donor.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.hospital.username)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.national_id,id2=item.hospital_id,id3=item.report_date }) |
            @Html.ActionLink("Details", "Details", new { id = item.national_id, id2 = item.hospital_id, id3 = item.report_date }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.national_id, id2 = item.hospital_id, id3 = item.report_date })
        </td>
    </tr>
}

</table>
